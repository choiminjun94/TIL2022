// Recap (복습)

const a = 5; 

// let은 업데이트가 가능, const는 불가능, var는 왠만해서는 사용하지 않기
// const isMinFat = true;
let isMinFat = true;

isMinFat = false;
console.log(isMinFat);

// null와 undefined
let hello;
console.log(hello); 

let hello2 = null;
console.log(hello2);
